query Category($id: ID!){
  category(id: $id) {
    name
    articles {
         id
      title
      name
      content
      image {
        url
      }
      category {
        id
        name
      }
      email
    }
  }
}
